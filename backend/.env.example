PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=mysql@2006
DB_NAME=course_allotment

# JWT — CHANGE THESE IN PRODUCTION (use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Optional: Preference deadline (ISO 8601 UTC)
# PREFERENCE_DEADLINE=2026-03-31T23:59:59.000Z

# Optional: Seed admin (used by seed-admin.ts)
# SEED_ADMIN_EMAIL=admin@nith.ac.in
# SEED_ADMIN_PASSWORD=admin@123
# SEED_ADMIN_NAME=Admin
